name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{de39706b-4873-48c4-8243-3e6ed7674193}}
          heroku_app_name: "sreetodoapp" #Must be unique in Heroku
          heroku_email: "sreekaanthg91@gmail.com"
